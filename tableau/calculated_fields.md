# Calculated fields and parameters

> Synthetic data from a seeded generator. Not real company, customer or financial results.

Generated by `python -m src.tableau.build` from the workbook specification. These are the exact formulas in `tableau/workbook/saas_revenue_intelligence.twbx`.

## Parameters

| Parameter | Type | Values | Default |
|---|---|---|---|
| Month | string | 36 values, 2023-01 to 2025-12 | 2025-12 |
| Risk threshold | real | 20 values, 0.01 to 0.2 | 0.04 |

## Calculated fields

| Data source | Field | Formula |
|---|---|---|
| KPIs by month | In selected month | `[month_label] = [Parameters].[month_param]` |
| MRR bridge | In selected month | `[month_label] = [Parameters].[month_param]` |
| Risk queue | At or above threshold | `[risk] >= [Parameters].[threshold_param]` |
| Threshold tradeoff | Selected threshold | `ABS([threshold] - [Parameters].[threshold_param]) < 0.0001` |
